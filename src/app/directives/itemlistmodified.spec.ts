import { Itemlistmodified } from './itemlistmodified';

describe('Itemlistmodified', () => {
  it('should create an instance', () => {
    const directive = new Itemlistmodified();
    expect(directive).toBeTruthy();
  });
});
